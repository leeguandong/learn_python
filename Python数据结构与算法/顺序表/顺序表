从0开始算？
因为非0数表示偏移量，0表示位于首位，不偏移

一个字节8位
类型不同，所占字节位数是不同的，整型占四个字节，char占一位
顺序表的两种形式：
基本形式：把顺序表按照顺序存放，方便存取
元素外置：可以实现不同类型的元素的存放，就是把元素的地址在存放到一个有序的地址块中，索引时索引有序地址块，通过索引去找元素地址

顺序表的结构
一个程序表的完整信息包括两部分，一部分是表中的元素集合，另一部分是为实现正确操作而需记录的信息，即有关表的整体情况的信息，这部分信息主要包括元素
存储区的容量和当前表中已有的元素个数两项
顺序表的两种实现方式
一体式和分离式
区别在于，一体式再给顺序表添加数据时，要重新申请内存空间，原先内存地址不可用，表头和数据体均不可用，但是在分离式中，表头是可以用的，只需要将表头最后一个
字节的地址换成新的数据体地址即可。

元素存储区扩充
申请固定数目
翻倍  用空间换时间，用空间牺牲了操作次数

顺序表的操作
增加元素和删除元素是一样的
表尾加入元素，时间复杂度为O(1)
保序的加入元素，时间复杂度为O(n)

list的基本实现技术
1.基于下标的高效元素的访问和更新，时间复杂度应该是O(1)
2.采用分离式的，元素外置，允许任何加入元素，而且不断加入元素过程中，表对象的标识不变
3.动态顺序表  执行插入的时候，一开始四倍四倍的增加，到了50000的时候，开始1倍1倍的增加
list内置操作的时间复杂度可以用顺序表来解释



