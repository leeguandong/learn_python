顺序表，将元素顺序的存放在一块连续的存储区里，元素间的顺序关系由他们存储顺序自然表示
链表，将元素存放在通过链接构造的一系列存储块中
这两种数据组织方式都成为线性表

栈：也称堆栈，是一种容器，可存入数据元素、访问元素、删除元素，它的特点在于只能允许在容器的一端(称为站顶端指标)进行加入数据和输出数据的运算。没有了
位置概念，保证任何时候可以访问、删除的元素都是此前最后存入的元素，确定了一种默认的访问顺序
后进先去

队列：队列是只允许在一端进行插入操作，而在另一端进行删除操作的线性表
先进先出

双端队列：是一种具有栈和队列性质的数据结构
双端队列中的元素可以从两端弹出，其限定插入和删除操作在表的两端进行。双端队列可以在队列任意一端入队和出兑
你可以从头部插入数据，也可以从头部取出数据，可以在尾部插入数据，也可以在尾部取出数据


